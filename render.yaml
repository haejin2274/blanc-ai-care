services:
  - type: web
    name: blanc-ai-care
    runtime: static
    buildCommand: 'echo "Static site"'
    staticPublishPath: ./
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
